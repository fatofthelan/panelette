#ifndef SECRETS_H
#define SECRETS_H

// Per-device WiFi credentials. This file is gitignored - do not commit it.
// Copy secrets.h.example to secrets.h and fill in real values.
#define WIFI_SSID     "your-wifi-ssid"
#define WIFI_PASSWORD "your-wifi-password"

#endif // SECRETS_H
